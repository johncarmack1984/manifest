# Rust
api/target/

# Node / pnpm
node_modules/
**/node_modules/
web/dist/
*.tsbuildinfo

# CDK
**/cdk.out/
infra/cdk-outputs.json

# Local config — never commit real values
infra/.env
**/.env

# Your real project registry — ships generic as projects.example.toml; your edited
# copy stays local (push it live with `just registry-push`).
api/projects.toml

# misc
.DS_Store
*.log
